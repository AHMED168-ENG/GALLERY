<style>
    .category select , .category input {
        width:100%;
        padding:10px
    }
    .category input {
        padding:7px
    }
    .category div {
        padding:0 !important
    }
    .category label {
        display: block !important;
    }
</style>
<%- include("../inclueds/head")  %>
</head>
<body>
<%- include("../inclueds/upperNavbare")  %> 
<%- include("../inclueds/lowerNavbare")  %> 
<!-- |||||||||||||||||||||||||||||||||||||||||||||||||||||| -->

    <!-- Breadcrumb Start -->
    <div class="container-fluid">
        <div class="row px-xl-5">
            <div class="col-12">
                <nav class="breadcrumb bg-light mb-30">
                    <a class="breadcrumb-item text-dark" href="/home">Home</a>
                    <a class="breadcrumb-item text-dark" href="/all-products">Shop</a>
                    <span class="breadcrumb-item active">Shoping Cart</span>
                </nav>
            </div>
            <div class="col-12">
                <div class="alert alert-danger">whene you mack the order we sent pdf as pilling to you</div>
            </div>

        </div>
    </div>
    <!-- Breadcrumb End -->


    <!-- Cart Start -->
    <div class="container-fluid">
        <div class="row px-xl-5">
              <div class="col-lg-12">
                <h5 class="section-title position-relative text-uppercase mb-3"><span class="bg-secondary pr-3">Order Total</span></h5>
                <div class="bg-light p-30 mb-5">
                    <div class="border-bottom">
                        <h6 class="mb-3 row ">
                            <span class="col-md-4">Products</span> 
                            <span class="col-md-4 text-center">Count</span> 
                            <span class="col-md-4 text-right">Real price</span> </h6>
                        <% allProductCart.forEach(element => { %>
                            <div class="row">
                                <p class="col-md-4"><%= element.cartProduct["productName_" + lang] %></p>
                                <p class="col-md-4 text-center"><%= element.count %>Eg</p>
                                <p class="col-md-4 text-right"><%= element.cartProduct.price %>Eg</p>
                            </div>                        
                        <% }) %>
                    </div>
                    <div class="border-bottom pt-3 pb-2">
                        <div class="d-flex justify-content-between mb-3">
                            <h6>structure</h6>
                            <h6><%= totalOfAll.structure %>Eg</h6>
                        </div>
                        <div class="d-flex justify-content-between">
                            <h6 class="font-weight-medium">Shipping</h6>
                            <h6 class="font-weight-medium"><%= totalOfAll.shipping %>Eg</h6>
                        </div>
                    </div>
        
                     <div class="pt-2">
                        <div class="d-flex justify-content-between mt-2">
                            <h5>Total Price Before Descount</h5>
                            <h5><%= totalOfAll.price %>Eg</h5>
                        </div>
                    </div>
                     <div class="pt-2">
                        <div class="d-flex justify-content-between mt-2">
                            <h5>Total After Descount </h5>
                            <h5><%= totalOfAll.afterDescount %>Eg</h5>
                        </div>
                    </div>
                    <div class="pt-2">
                        <div class="d-flex justify-content-between mt-2">
                            <h5>Total After Descount with & shipping & structure</h5>
                            <h5><%= totalOfAll.totalPrice %>Eg</h5>
                        </div>
                    </div>
                   
                </div>
                <div class="mb-5">
                    <div class="bg-light p-30">
                        <form action="?_csrf=<%= csrf %>" method="post">
                            <button onclick="confirm('Are you sure you want to order this items?')" class="btn btn-block btn-primary font-weight-bold py-3">Order Now</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Cart End -->


<%- include("../inclueds/footer")  %>
<%- include("../inclueds/footer_script")  %>
<script src="/public/website/js/sweetalert.min.js"></script>
<script>

    const UserCart = document.querySelector(".UserCart")
    const addUserCart = document.querySelectorAll(".addUserCart")
    addUserCart.forEach(ele => {
        ele.onclick = (e) => {
            e.preventDefault()
            const productId = ele.getAttribute("data_attr")
            ele.classList.toggle("active")
                $.ajax({
                url : "/add-to-shoping-cart?_csrf=" + '<%= csrf %>',
                method:"post",
                data : {
                    userId : '<%= userData ? userData.id : 0 %>',
                    productId,
                    count:1,
                },
                success(data) {
                    if(data.status) {
                        if(ele.classList.contains("active")){
                            UserCart.innerHTML = +UserCart.innerHTML + 1
                        } else {
                            UserCart.innerHTML = +UserCart.innerHTML - 1
                        }
                        document.getElementById(productId).remove()
                        Swal.fire({
                            icon: 'success',
                            title:data.message,
                            showConfirmButton: false,
                            timer: 2500
                        })
                        setTimeout(() => {
                            window.location.reload()
                        } , 2500)
                    } else {
                        Swal.fire({
                            icon: 'error',
                            title:data.message,
                            showConfirmButton: false,
                            timer: 2500
                        })                    
                    }
                },
                error(error){
                    Swal.fire({
                            icon: 'error',
                            title:"505 error",
                            showConfirmButton: false,
                            timer: 2500
                        })                
                    }
            })
        }
    })
 


    // change product count in cart
    function changeCountByButton(productId , operation) {
        const input = document.querySelector(`[data_attr='${productId}']`)
        console.log(input)
        if(operation == "plus") {
            input.value = +input.value + 1
        } else {
            if(input.value == 1) return
            input.value = +input.value - 1
        }
        sendAjaxCall(productId , input.value)
    } 
    
    function changeCountByInput(e ,productId) {
        const count = e.target.value
        if(!count || count == 0) return
        sendAjaxCall(productId , count)
    } 
    
 function sendAjaxCall(productId , count) {
    $.ajax({
        url : "/shoping-cart-count?_csrf=" + '<%= csrf %>',
        method:"post",
        data : {
            userId : '<%= userData ? userData.id : 0 %>',
            productId,
            count:count,
        },
        success(data) {
            if(data.status) {
                Swal.fire({
                    icon: 'success',
                    title:data.message,
                    showConfirmButton: false,
                    timer: 2500
                })
                setTimeout(() => {
                    window.location.reload()
                } , 2500)
            } else {
                Swal.fire({
                    icon: 'error',
                    title:data.message,
                    showConfirmButton: false,
                    timer: 2500
                })                    
            }
        },
        error(error){
            Swal.fire({
                    icon: 'error',
                    title:"505 error",
                    showConfirmButton: false,
                    timer: 2500
                })                
            }
        })
 }

</script>
</body>
    